Place your logo image here as `logo.png`.

Steps:
1. Add your logo file at `assets/images/logo.png` (recommended PNG, 32–128 px height).
2. Run `flutter pub get`.
3. Re-run the app with `flutter run`.

If no image is present, the app will show the default Flutter logo as a fallback.